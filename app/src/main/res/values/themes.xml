<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TasteBuds" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Header/ActionBar styling - darker and more inviting -->
        <item name="colorPrimary">@color/header_background</item>
        <item name="colorOnPrimary">@color/header_text_color</item>
        <item name="colorPrimaryContainer">@color/header_background</item>
        <item name="colorOnPrimaryContainer">@color/header_text_color</item>

        <!-- ActionBar specific styling -->
        <item name="actionBarStyle">@style/CustomActionBar</item>
        <item name="toolbarStyle">@style/CustomToolbar</item>

        <!-- Make action bar icons visible -->
        <item name="actionMenuTextColor">@color/header_icon_color</item>
    </style>

    <style name="Theme.TasteBuds" parent="Base.Theme.TasteBuds" />

    <!-- Custom ActionBar style -->
    <style name="CustomActionBar" parent="Widget.AppCompat.ActionBar.Solid">
        <item name="background">@color/header_background</item>
        <item name="titleTextStyle">@style/CustomActionBarTitle</item>
    </style>

    <!-- Custom Toolbar style -->
    <style name="CustomToolbar" parent="Widget.Material3.Toolbar">
        <item name="android:background">@color/header_background</item>
        <item name="titleTextColor">@color/header_text_color</item>
        <item name="subtitleTextColor">@color/header_text_color</item>
    </style>

    <!-- ActionBar title text style -->
    <style name="CustomActionBarTitle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/header_text_color</item>
        <item name="android:textSize">20sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>
</resources>